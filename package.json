{
  "name": "boardvault",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build --filter=web",
    "dev": "turbo run dev --filter=web",
    "lint": "turbo run lint --filter=web"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}